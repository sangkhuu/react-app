name: Hello World
author: Sangkhuu
description: Hello there,

inputs:
  who-to-greet:
    description: 'Who to greet'
    required: true
    default: Sangkhuu

outputs:
  time:
    description: 'The greeting time'

runs:
  using: 'docker'
  image: "Dockerfile"
  # entrypoint:
  args:
    - ${{ inputs.who-to-greet }}